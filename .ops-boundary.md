# Ops Boundary: LynxAuth

Project path: /home/meowlabs/lynxauth

Boundary rule: this project is isolated from other VPS projects unless an explicit relationship is documented in env, compose, systemd, nginx, deploy scripts, or project docs.

Verified notes:
- source of truth includes docker-compose.yml in this root
- compose DB hostname is postgres inside project network
- loopback ports include 8082 and 3001
- postgres host mapping is 55432 -> container 5432

Do not:
- infer cross-project relationships from similar names, ports, containers, or folders
- reuse another project's database, Redis, Docker network, Docker volume, or host port without explicit source-of-truth evidence
- modify another project's systemd service, PM2 app, nginx config, env file, deploy script, or container while working here
- delete project files, containers, volumes, or dependencies unless obsolete status is confirmed and deletion is explicitly approved
- rename DB host randomly when the real issue is a missing project dependency
- repoint LynxAuth to AgentWA or OneDock databases without explicit documentation

Mandatory preflight before any write/change here:
- ss -tulpen
- docker ps -a
- docker network ls
- docker volume ls
- pm2 ls
- systemctl list-units --type=service
- inspect target env/compose/systemd/deploy source
- inspect related nginx config if this project has a hostname or reverse proxy

Decision rule: if ownership or dependency is ambiguous, stop and verify. Do not borrow infra from another project by guesswork.
